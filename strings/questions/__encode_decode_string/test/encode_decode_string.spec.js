
import { expect } from 'chai';
import { encode, decode } from '../encode_decode_string';

describe('encode()', () => {
  // it('should', () => {
  //
  // });
});

describe('decode()', () => {
  // it('should', () => {
  //
  // });
});
