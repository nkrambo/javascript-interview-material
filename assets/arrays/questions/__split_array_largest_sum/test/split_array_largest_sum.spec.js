
import { expect } from 'chai';
import splitArray from '../split_array_largest_sum';

describe('splitArray()', () => {
  // it('should split an array into m non-empty continuous sub arrays', () => {
  //   expect(splitArray([7, 2, 5, 10, 8], 2)).to.equal(18);
  // });
});
