
/**
* Plus One
*
* Tags: Array, Math
* Leetcode: 66
*
* Given a non-negative integer represented as a non-empty array of digits, plus
* one to the integer.
*
* You may assume the integer do not contain any leading zero, except the number
* 0 itself.
*
* The digits are stored such that the most significant digit is at the head of
* the list.
*/

/**
* plusOne()
*
* Solution:
*
* Time: O(1)
* Space: O(1)
*
* @param {array} digits non-empty array of digits
* @return {array} returns digits plus one
*/

// function plusOne(digits) {
//
// }
//
// export default plusOne;
