
// import productExceptSelf from '../product_of_array_except_self';

describe.skip('productExceptSelf()', () => {
  test('should return the product of all other elements', () => {
    // expect(productExceptSelf([1, 7, 3, 4])).toEqual([84, 12, 28, 21]);
    // expect(productExceptSelf([1, 1, 1])).toEqual([1, 1, 1]);
  });
});
