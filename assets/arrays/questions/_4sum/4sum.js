
/**
* 4Sum
*
* Tags: Array, Map, Two Pointer
* Leetcode: 18
*
* Given an array S of n integers, are there elements a, b, c and d in S such that
* a + b + c + d = target? Find all unique quadruplets in the array which gives the
* sum of target.
*
* Note:
*
* The solution set must not contain duplicate quadruplets.
*
* Example:
*
* Given [1, 0, -1 0, -2, 2], and target 0
* Return
* [[-1, 0, 0, 1],
*  [-2, -1, 1, 2],
*  [-2, 0, 0, 2]]
*/

/**
* fourSum()
*
* Solution:
*
*
* Time: O(1)
* Space: O(1)
*
* @param {array} nums positive and negative integers
* @param {number} target to sum to
* @return {array} returns an array quadruplets values that sum to target
*/

// function fourSum(nums, target) {
//
// }
//
// export default fourSum;
