
import { expect } from 'chai';
import twoSum from '../two_sum';

describe('twoSum()', () => {
  // it('should find all unique pairs which sum to target', () => {
  //   expect(twoSum([2, 7, 11, 15], 9)).to.deep.equal([0, 1]);
  // });
  //
  // it('should throw an error if nums is less than 2', () => {
  //   expect(function(){ twoSum([1, 2]); }).to.throw(Error);
  // });
  //
  // it('should throw a type error', () => {
  //   expect(function(){ twoSum(null); }).to.throw(TypeError);
  //   expect(function(){ twoSum(undefined); }).to.throw(TypeError);
  //   expect(function(){ twoSum('string'); }).to.throw(TypeError);
  // });
});
