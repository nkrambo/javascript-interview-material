
import { expect } from 'chai';
import searchRotatedArray from '../search_in_rotated_array';

describe('searchRotatedArray()', () => {
  it('should find an element in a rotated sorted array', () => {
    // const arr = [15, 16, 19, 20, 25, 1, 3, 4, 5, 7, 10, 14];
    // expect(searchRotatedArray(arr, 5)).to.equal(8);
  });
});
