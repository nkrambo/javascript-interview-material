
// import solveNQueens from '../n_queens';

describe.skip('solveNQueens()', () => {
  test('should return an array of strings with all queens positioned so that none attack any other queens', () => {
    const solved = solveNQueens(4);
    expect(solved).toEqual();
  });
});
