
// import radixSort from '../radix_sort';

describe.skip('radixSort()', () => {
  test('should return a new sorted array', () => {
    // expect(bubbleSort([4, 3, 7, 1, 2, 5, 9])).toEqual([1, 2, 3, 4, 5, 7, 9]);
    // expect(bubbleSort([3, 5, 3, 3, 5])).toEqual([3, 3, 3, 5, 5]);
  });
});
