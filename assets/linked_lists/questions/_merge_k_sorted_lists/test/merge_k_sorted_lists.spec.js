
import mergeKLists from '../merge_k_sorted_lists';
import { ListNode, addNode } from '../../../data_structures/list_node/list_node';

describe('mergeKLists()', () => {
  test('should return the head list node of k merged linked lists', () => {

  });
});
