
// import { swapPairsRecursive, swapPairs } from '../swap_nodes_in_pairs';

describe.skip('swapPairsRecursive()', () => {
  test('should swap every pair of adjacent nodes and return the list head', () => {
    // // 1 -> 2 -> 3 -> 4
    // const l1 = new LinkedList();
    // l1.add(1, 0);
    // l1.add(2, 1);
    // l1.add(3, 2);
    // l1.add(4, 3);
    //
    // // 2 -> 1 -> 4 -> 3
    // const swapped = swapPairsRecursive(l1.head);
    // expect(swapped.value).toBe(2);
    // expect(swapped.next.value).toBe(1);
    // expect(swapped.next.next.value).toBe(4);
  });
});

describe.skip('swapPairs()', () => {
  test('should swap every pair of adjacent nodes and return the list head', () => {
    // // 1 -> 2 -> 3 -> 4
    // const l2 = new LinkedList();
    // l2.add(1, 0);
    // l2.add(2, 1);
    // l2.add(3, 2);
    // l2.add(4, 3);
    //
    // // 2 -> 1 -> 4 -> 3
    // const swapped = swapPairs(l2.head);
    // expect(swapped.value).toBe(2);
    // expect(swapped.next.value).toBe(1);
    // expect(swapped.next.next.value).toBe(4);
  });
});
