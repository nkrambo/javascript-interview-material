
import { expect } from 'chai';
import tripleStep from '../triple_step';

describe('tripleStep()', () => {
  // it('should count the number of ways a child can run up the stairs with 1, 2 or 3 steps at a time', () => {
  //   expect(tripleStep()).to.equal();
  // });
});
