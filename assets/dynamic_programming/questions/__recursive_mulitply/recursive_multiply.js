
/**
* Recursive Multiply
*
* Write a recursive function to multiply to positive integers without using the
* '*' operator. You can use addition, subtraction, and bit shifting, but you should
* minimise the use of those operations.
*
* Solution:
*
* Time: O(1)
* Space: O(1)
*
* @param {number} a integer to multiply
* @param {number} b integer to multiply
* @return {number} returns the multiplication of a and b
*/

function recursiveMultiply(a, b) {

}

export default recursiveMultiply;
