
/**
* Magic Index
*
* A magic index in an array A[0... n-1] is defined to be an index such that A[i] = i.
* Given a sorted array of distinct integers, write a method to find the magic index,
* if one exists, in the array A.
*
* Solution:
*
* Time: O(1)
* Space: O(1)
*
* @param {array} arr increasing positive integers 1..n
* @return {number} returns index where arr[i] = i
*/

function magicIndex(arr) {

}

export default magicIndex;
