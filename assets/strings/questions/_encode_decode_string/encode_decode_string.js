
/**
* Encode and Decode Strings
*
* Design an algorithm to encode a list of strings to a string. The encoded
* string is then sent over the network and is decoded back to the original
* list of strings.
*
* Machine 1 (sender) has the function:
*
* function encode(array) {
*   // ... your code
*   return 'encoded_string';
* }
*
* Machine 2 (receiver) has the function:
*
* function decode(string) {
*   //... your code
*   return ['string', 'string'];
* }
*
* Implement the encode and decode methods.
*
* Notes:
*
* - The string may contain any possible characters out of 256 valid ascii
*   characters. Your algorithm should be generalized enough to work on any
*   possible characters.
*
* - Do not use class global variables to store states. Your encode and decode
*   algorithms should be stateless.
*
* - Do not rely on any library methods. You should implement your own
*   encode/decode algorithm.
*/

/**
* encode()
* Encodes a list of strings to a single string.
*
* Solution:
*
* Time: O(1)
* Space: O(1)
*
* @param {array} strings array of strings to be encoded
* @return {string} returns a single encoded string
*/

// function encode(strings) {
//
// }

/**
* decode()
* Decodes a single string to a list of strings.
*
* Solution:
*
* Time: O(1)
* Space: O(1)
*
* @param {string} str single string of (n) length to be decoded
* @return {array} returns array of decoded strings
*/

// function decode(str) {
//
// }
//
// export { encode, decode };
