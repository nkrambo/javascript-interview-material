
// import { canJump, canJumpAlt } from '../jump_game';

describe.skip('canJump()', () => {
  test('should return true if the last index can be reached', () => {
    // expect(canJump([2, 3, 1, 1, 4])).toBeTruthy();
    // expect(canJump([1, 0])).toBeTruthy();
    // expect(canJump([1, 1, 1, 1])).toBeTruthy();
  });

  test('should return false if the last index can NOT be reached', () => {
    // expect(canJump([3, 2, 1, 0, 4])).toBeFalsy();
    // expect(canJump([0, 1])).toBeFalsy();
    // expect(canJump([1, 1, 0, 4])).toBeFalsy();
  });
});

describe.skip('canJumpAlt()', () => {
  test('should return true if the last index can be reached', () => {
    // expect(canJumpAlt([2, 3, 1, 1, 4])).toBeTruthy();
    // expect(canJumpAlt([1, 0])).toBeTruthy();
    // expect(canJumpAlt([1, 1, 1, 1])).toBeTruthy();
  });

  test('should return false if the last index can NOT be reached', () => {
    // expect(canJumpAlt([3, 2, 1, 0, 4])).toBeFalsy();
    // expect(canJumpAlt([0, 1])).toBeFalsy();
    // expect(canJumpAlt([1, 1, 0, 4])).toBeFalsy();
  });
});
