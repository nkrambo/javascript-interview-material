
import { expect } from 'chai';
import insertion from '../insertion';

describe('insertion()', () => {
  // it('should insert m into n such that m starts at bit j and ends at bit i', () => {
  //   expect(insertion(10000000000, 10011, 2, 6)).to.equal(10001001100);
  // });
});
