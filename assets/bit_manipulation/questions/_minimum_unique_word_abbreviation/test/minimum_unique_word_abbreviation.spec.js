
// import minAbbreviation from '../minimum_abbreviation';

describe.skip('minAbbreviation()', () => {
  test('should return the smallest possible abbreviation of the target which does not conflict with other strings in the dictionary', () => {
    // expect(minAbbreviation('apple', ['blade'])).toBe('a4');
  });
});
