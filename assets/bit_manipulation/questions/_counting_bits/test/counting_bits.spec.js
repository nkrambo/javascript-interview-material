//

// import countBits from '../counting_bits';
//
// describe('countBits()', () => {
//   test('should return the count of 1 bits for every number in the range 0 ≤ i ≤ num', () => {
//     expect(countBits(5)).to.deep.equal([0, 1, 1, 2, 1, 2]);
//     expect(countBits(0)).to.deep.equal([0]);
//   });
// });
