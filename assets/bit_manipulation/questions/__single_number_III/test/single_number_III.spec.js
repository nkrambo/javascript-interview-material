
import { expect } from 'chai';
import singleNumberIII from '../single_number_III';

describe('singleNumberIII()', () => {
  // it('should return an array of values only appearing once in a sequence of duplicate values', () => {
  //   expect(singleNumberIII([1, 2, 1, 3, 2, 5])).to.deep.equal([3, 5]);
  // });
});
