
// import missingNumber from '../missing_number';

describe.skip('missingNumber()', () => {
  test('should return the missing number from the triangular series', () => {
    // expect(missingNumber([0, 1, 2, 4, 5, 6, 7])).toBe(3);
    // expect(missingNumber([1, 2, 3, 4, 5, 6, 7])).toBe(0);
    // expect(missingNumber([0, 1, 2, 3, 4, 5, 7])).toBe(6);
  });
});
