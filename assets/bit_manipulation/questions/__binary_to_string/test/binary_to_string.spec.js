
import { expect } from 'chai';
import binaryToString from '../binary_to_string';

describe('binaryToString()', () => {
  // it('should return the binary representation of a real number between 0 and 1', () => {
  //   expect(binaryToString()).to.equal();
  // });
});
