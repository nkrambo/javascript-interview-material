
// import integerReplacement from '../integer_replacement';

describe.skip('integerReplacement()', () => {
  test('should return the minimum number of replacements needed for num to become 1 if num is even', () => {
    // expect(integerReplacement(8)).toBe(3);
  });

  test('should return the minimum number of replacements needed for num to become 1 if num is odd', () => {
    // expect(integerReplacement(7)).toBe(4);
  });
});
