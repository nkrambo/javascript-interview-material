
// import buildTree from '../construct_binary_tree_from_inorder_and_postorder_traversal';

describe.skip('buildTree()', () => {
  test('should construct a BT from a in-order and post-order traversal', () => {
    const inOrder = [];
    const postOrder = [];

    const tree = buildTree(inOrder, postOrder);

    expect(tree.val).toBe();
  });
});
