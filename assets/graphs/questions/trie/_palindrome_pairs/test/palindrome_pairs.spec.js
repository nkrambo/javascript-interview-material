
// import { palindromePairs, palindromePairsHash } from '../palindrome_pairs';

describe.skip('palindromePairs()', () => {
  test('should return an array of valid indices for palidrome pairs', () => {
    // expect(palindromePairs(['bat', 'tab', 'cat'])).toEqual()([[1, 0], [0, 1]]);
    // expect(palindromePairs(['abcd', 'dcba', 'lls', 's', 'sssll'])).toEqual()([[1, 0], [0, 1], [3, 2], [2, 4]]);
    // expect(palindromePairs(['dad', ''])).toEqual()([[0, 1]]);
  });
});

describe.skip('palindromePairsHash()', () => {
  test('should return an array of valid indices for palidrome pairs', () => {
    // expect(palindromePairsHash(['bat', 'tab', 'cat'])).toEqual()([[1, 0], [0, 1]]);
    // expect(palindromePairsHash(['abcd', 'dcba', 'lls', 's', 'sssll'])).toEqual()([[1, 0], [0, 1], [3, 2], [2, 4]]);
    // expect(palindromePairsHash(['dad', ''])).toEqual()([[0, 1]]);
  });
});
