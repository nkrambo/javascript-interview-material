
// import canFinish from '../course_schedule';

describe.skip('canFinish()', () => {
  test('should return true if the course schedule can be completed', () => {

  });

  test('should return false if the course schedule can NOT be completed', () => {

  });
});
