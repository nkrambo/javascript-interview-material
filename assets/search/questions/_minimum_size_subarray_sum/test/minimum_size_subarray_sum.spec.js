
// import minSubArrayLen from '../minimum_size_subarray_sum';

describe.skip('minSubArrayLen()', () => {
  test('should return the length of the minimum size subarray sum', () => {

  });

  test('should return 0 if there is no minimum size subarray sum', () => {

  });
});
