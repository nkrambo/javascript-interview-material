
// import sparseSearch from '../sparse_search';

describe.skip('sparseSearch()', () => {
  test('should return the index of target if it exists in words array', () => {
    // const words = ['test', 'at', '', '', 'ball', '', '', 'car', '', '', 'dad', '', 'jim'];
    // expect(sparseSearch(words, 'ball')).toBe(4);
    // expect(sparseSearch(words, 'at')).toBe(1);
    // expect(sparseSearch(words, 'test')).toBe(0);
    // expect(sparseSearch(words, 'dad')).toBe(10);
    // expect(sparseSearch(words, 'jim')).toBe(12);
  });

  test('should return -1 if target does not exist', () => {
    // const words = ['at', '', '', '', 'ball', '', '', 'car', '', '', 'dad', '', ''];
    // expect(sparseSearch(words, 'notfound')).toBe(-1);
  });
});
