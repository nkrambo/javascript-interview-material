
import { expect } from 'chai';
import { commonAncestorWithParents, commonAncestor } from '../first_common_ancestor';

describe('commonAncestorWithParents()', () => {
  // it('should', () => {
  //
  // });
});

describe('commonAncestor()', () => {
  // it('should', () => {
  //
  // });
});
