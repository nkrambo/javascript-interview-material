
/**
* Group Anagrams
*
* Write a method to sort an array of strings so that all
* the anagrams are next to each other.
*
*/

/**
* Group anagrams
*
* Solution: Anagrams are words that have the same characters but in different orders.
*
* @param {array} arr array of strings
* @return {array} Returns a sorted array of strings with anagrams next to each other
*/

function groupAnagrams(arr) {

}

export default groupAnagrams;
